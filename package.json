{
  "name": "oliv-e-website-playwright",
  "version": "1.0.0",
  "description": "E2E test suite for the Oliv-e website using Playwright — covers navigation, all product pages, blog, contact forms, mobile responsiveness and accessibility.",
  "scripts": {
    "test": "playwright test --project=website",
    "test:ui": "playwright test --project=website --ui",
    "test:headed": "playwright test --project=website --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
